# Cloudflare Worker Proxy URL
# ============================
# 部署代理后，将下面的 URL 替换为你的 Worker 地址。
# 例如: https://desktop-pet-pixel-proxy.your-subdomain.workers.dev
#
# 部署步骤:
#   cd proxy
#   npx wrangler secret put OPENAI_API_KEY   # 粘贴你的 Agnes AI API Key
#   npx wrangler deploy                      # 部署后获得 URL
#
# 不配置此项时，应用会使用本地 Pillow 像素化作为回退。

PIXEL_PROXY_URL=https://desktop-pet-pixel-proxy.你的子域名.workers.dev
