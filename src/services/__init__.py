"""Service layer — business logic independent of UI.

Services:
    DatabaseService — persistence (QSettings wrapper)
    ImageService    — image validation, path utilities
    AIService       — AI processing unified entry point
    PetService      — pet lifecycle, hunger, state coordination
"""
